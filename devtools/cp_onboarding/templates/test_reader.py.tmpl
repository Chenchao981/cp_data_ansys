"""Acceptance-oriented tests for {{COMPANY_NAME}} format {{FORMAT_VERSION}}."""

import pytest


def test_replace_with_approved_contract_assertions():
    pytest.fail("Replace scaffold with golden-sample assertions before integration")
